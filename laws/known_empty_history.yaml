# Known-intractable empty-history cache entries.
#
# Entries must include the fields validated by laws/history_allowlist.py and
# must only be added after direct lawSearch, lsHistory, and lawService audits.
# The previous entries were removed on 2026-08-24 after shorter, exact-filtered
# lsHistory fallback queries recovered every document and amendment history.

entries: []
